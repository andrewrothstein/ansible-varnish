---
- name: add gpg key...
  become: yes
  become_user: root
  apt_key:
    url: '{{ varnish_apt_key_url }}'
    state: present
- name: add apt repo...
  become: yes
  become_user: root
  apt_repository:
    repo: '{{ varnish_apt_repo }}'
